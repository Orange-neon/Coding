#include<bits/stdc++.h>
using namespace std;

bool Works(long long h, long long k, vector<long long>& logs) {
    long long numLog = 0;
    for (long long i : logs) {
        numLog += h / i;
    }
    return (numLog >= k);
}

int main() {
    ios::sync_with_stdio(0);
    cin.tie(0);
    //freopen("1.in", "r", stdin);
    long long n, k;
    vector<long long> logs;
    cin >> n >> k;
    for (long long i = 0; i < n; ++i) {
        long long x;
        cin >> x;
        logs.push_back(x);
    }
    long long maxLog = k * (*min_element(logs.begin(), logs.end()));
    long long i = 0, j = maxLog;
    while (i <= j) {
        long long mid = (i + j) / 2;
        if (Works(mid, k, logs)) {
            j = mid - 1;
        } else {
            i = mid + 1;
        }
    }
    cout << i;
}
